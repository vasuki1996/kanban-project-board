apply plugin: "war"

bootWar {
    mainClassName = "com.xlabs.projectboard.ProjectBoardApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
